# Inventory System Backend

This is the backend for the inventory system. Built with FastAPI, it manages inventory data and provides RESTful endpoints.

## Getting Started

1. Clone the repository.
2. Install dependencies with `pip install -r requirements.txt`.
3. Run the application with `uvicorn main:app --reload`.

Ensure your environment variables are correctly set for database connection and CORS.